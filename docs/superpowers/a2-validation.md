# Plan A2 validation

## TTFT fidelity probe (pending controller run)

To be filled by the controller: one tiny `claude -p` probe through claude-tap → ttft_proxy,
confirming (a) a TTFT row is recorded and (b) tap-captured usage matches the session JSONL
(caching not perturbed).
